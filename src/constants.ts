export enum Statuses {
    LOADING = 'loading',
    LOADED = 'loaded',
    ERROR = 'error',
}

export enum ApiUrls {
    POSTS = 'https://jsonplaceholder.typicode.com/posts',
    FAKE_IMG = 'https://fakeimg.pl/300x150',
}
